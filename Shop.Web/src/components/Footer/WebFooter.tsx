import * as React from 'react';

import './WebFooter.css';

interface IProps {

}

const WebFooter = (props: IProps) => {

    return (
        <div>
            <h1>WebFooter</h1>
        </div>
    );
}

export default WebFooter;